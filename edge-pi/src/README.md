# edge-pi/src

엣지 실행 코드(카메라/AI 추론/센서 융합/제동 제어/통신)의 소스가 들어가는 영역이다.

## 호환성
- 이벤트/제어 페이로드 필드는 v1 계약에 맞춘다.
- `ttlMs`와 ACK 추적 흐름을 반드시 지킨다.

## 확장성
- 모듈 단위 교체(예: 카메라 파이프라인 변경, AI 모델 변경)는 디렉터리 경계를 넘지 않게 만든다.

## 가시성/명확성
- 각 모듈은 입력/출력을 명확히 문서화하고, 센서 윈도우/중복 제거 정책을 로컬 로그로 남긴다.

