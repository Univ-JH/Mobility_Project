"""
하드웨어 제어 설정 파일
모든 핀 번호는 BCM(GPIO 번호) 기준
"""

# ==========================================
# 1. GPIO 핀 번호 할당(라즈베리파이)
# ==========================================
SERVO_PIN = 18       # 브레이크 제어용 서보모터 (물리 12번 핀)

# ==========================================
# 2. 서보모터 (브레이크) 교정값 세팅
# ==========================================
SERVO_MIN_PULSE = 850   # 브레이크 완전 해제 (0도)
SERVO_MAX_PULSE = 2150  # 브레이크 완전 작동 (180도)
PWM_FREQ = 50           # 서보모터 표준 주파수 (Hz)
BRAKE_HOLD_TIME = 1.0   # 모터 이동 후 힘을 유지하는 시간 (초)

# ==========================================
# 3. 위치 및 모션 센서 통신 설정 (GPS & IMU)
# ==========================================
GPS_SERIAL_PORT = "/dev/serial0"
GPS_BAUDRATE = 9600

IMU_I2C_ADDRESS = 0x68
IMU_SHOCK_THRESHOLD = 2.5  # 2.5G 이상의 충격을 받으면 사고로 간주

# ==========================================
# 4. AI 비전 판단 기준값
# ==========================================
VISION_CONFIDENCE_THRESHOLD = 0.50  # 50% 미만의 확신도면 속도 감속 (Fail-Safe)
WALK_SPEED_THRESHOLD = 5.0          # 5.0km/h 미만은 '끌바' 또는 '정차'로 간주 (인도 진입 허용)