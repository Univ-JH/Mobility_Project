# frontend-web/src/hooks

재사용 가능한 React 훅(예: 장치 상태 구독, 이벤트 목록 조회, 응급 케이스 액션)을 모으는 영역이다.

## 호환성
- 데이터 계약은 `frontend-web/src/api/` 또는 공유 타입을 기준으로 고정한다.

## 확장성
- MQTT/WS 방식이 바뀌면 hook 내부 구현만 교체하고 화면 API는 유지한다.

## 가시성/명확성
- 훅은 “데이터 처리”와 “상태 노출”을 분리한다(렌더링 로직에 비즈니스가 섞이지 않게).

