# frontend-web/src/screens

페이지 단위(대시보드, 장치 목록, 주행 이력 상세, 응급 케이스 화면 등) UI를 담당한다.

## 호환성
- 응급/이벤트 UX는 `caseId`(또는 backend가 제공하는 동일 식별자)를 기준으로 중복 표시를 방지한다.

## 확장성
- 새로운 정책/알림 기능은 기존 상태 화면 패턴(리스트 + 상세 + 액션)을 따른다.

## 가시성/명확성
- 각 화면은 로딩/에러/준비 상태를 명확히 분리한다.

