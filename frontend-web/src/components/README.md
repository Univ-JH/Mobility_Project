# frontend-web/src/components

재사용 가능한 UI 컴포넌트(버튼, 카드, 상태 배지, 알림 뱃지 등) 영역이다.

## 호환성
- 이벤트/상태 표현 컴포넌트는 `severity` enum(`low|medium|high`)을 기준으로 색/표시 정책을 고정한다.

## 확장성
- 새 이벤트 타입이 추가되어도 컴포넌트는 “타입 문자열”을 직접 가정하지 않고 공통 모델로 렌더한다.

## 가시성/명확성
- 컴포넌트는 “표시”만 담당하고, 데이터 패칭/스트림 파싱 로직은 상위 레이어로 둔다.

