# frontend-web/src/state

클라이언트 상태 관리(서버 상태 캐시/전역 UI 상태)를 위한 영역이다.

## 호환성
- 서버 상태(이벤트/목록)는 캐시 무효화 전략을 갖추고, 계약 기반 파서만 허용한다.

## 확장성
- 전역 상태 라이브러리가 바뀌어도 도메인 데이터 구조(이벤트/상태 모델)는 동일하게 유지한다.

## 가시성/명확성
- 상태 전이(loading/error/ready)는 항상 명시적으로 표현한다.

