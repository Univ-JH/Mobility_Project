# embedded-helmet/src/ble

BLE 프로토콜, 연결 관리, 페이로드 인코딩/디코딩을 담당하는 영역이다.

## 호환성
- 안전 이벤트는 서버에서 필요한 최소 필드(`seq`, `timestamp`, `rideId`)를 포함한다.

## 확장성
- 서버/엣지 계약 변경 시 `schemaVersion`을 우선 분기하고 하위 호환을 고려한다.

## 가시성/명확성
- BLE 연결 상태 변화는 로컬 로그와(가능하면) 이벤트로 남겨 서버 추적을 돕는다.

