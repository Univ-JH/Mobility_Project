# frontend-mobile/src/hooks

모바일에서 재사용 가능한 훅(실시간 상태 구독, 이벤트 목록 조회, 응급 액션 핸들러 등)을 담는 영역이다.

## 호환성
- 데이터/계약 파싱은 `api/` 또는 공유 타입을 기준으로 한다.

## 확장성
- 백엔드 이벤트 타입이 추가되면 훅에서만 파싱 로직을 확장하고 화면 로직은 최대한 유지한다.

## 가시성/명확성
- 훅은 “데이터 처리”와 “상태 노출”을 분리한다.

