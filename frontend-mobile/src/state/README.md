# frontend-mobile/src/state

모바일 클라이언트의 상태 관리(서버 캐시, UI 상태, 응급 케이스 중복 방지 캐시 등)를 담당한다.

## 호환성
- 이벤트 중복 방지는 `seq` 또는 `caseId`를 기준으로 한다(계약 기반).

## 확장성
- 오프라인 캐시/동기화가 늘어나면 state 계층의 책임 범위를 조정한다.

## 가시성/명확성
- 앱 상태 전이(예: 포그라운드/백그라운드, 연결 상태)는 별도 상태로 표현한다.

