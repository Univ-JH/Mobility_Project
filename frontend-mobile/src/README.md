# frontend-mobile/src

모바일 앱의 실제 구현 소스가 들어갈 디렉터리다.

## 호환성
- 백엔드 API 응답 envelope와 에러코드 규칙을 그대로 따른다.

## 확장성
- 실시간 스트림/푸시 알림/오프라인 캐시 등 기능이 늘어나면 `api/`와 `services/`로 분리한다.

## 가시성/명확성
- 앱 상태(포그라운드/백그라운드)에 따른 네트워크 정책을 코드 구조로 분리한다.

