# frontend-mobile/src/components

모바일 공통 UI 컴포넌트(상태 배지, 알림 카드, 버튼 등)를 담는 영역이다.

## 호환성
- 이벤트 `severity` enum(`low|medium|high`)에 따라 표시 정책을 고정한다.

## 확장성
- 새로운 이벤트 타입이 추가되어도 컴포넌트는 “표시” 중심으로 유지한다.

## 가시성/명확성
- 데이터 파싱/중복제거는 컴포넌트 외부(훅/서비스)로 분리한다.

